<?xml version="1.0" encoding="UTF-8"?>

<project name="common_test" default="dummy">
    <import file="../../common.xml"/>
    <property name="project.dir.behat" value="${phing.dir.common_test}/tmp"/>

    <target name="test:setup">
        <mkdir dir="${project.dir.behat}"/>
    </target>
    <target name="test:teardown">
        <delete dir="${project.dir.behat}"/>
    </target>
</project>